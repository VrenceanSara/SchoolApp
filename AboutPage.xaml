<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SchoolApp.AboutPage"
             Title="AboutPage">
    <ScrollView>
        <StackLayout Padding="20">
            <Label Text="🎵 Music School 🎵"
                   FontSize="24"
                   FontAttributes="Bold"
                   HorizontalOptions="Center"/>

            <Image Source="music_logo.png"
                   HeightRequest="150"
                   WidthRequest="150"
                   HorizontalOptions="Center"/>

            <Label Text="Aplicația Music School este creată pentru a gestiona programările, membrii și instrumentele unei școli de muzică. Aceasta oferă funcționalități precum:"
                   FontSize="16"
                   Margin="0,10,0,10"/>

            <Label Text="✔ Autentificare și autorizare bazată pe roluri"
                   FontSize="14"/>
            <Label Text="✔ Gestionarea studenților și profesorilor"
                   FontSize="14"/>
            <Label Text="✔ Programarea lecțiilor de muzică"
                   FontSize="14"/>
            <Label Text="✔ Alocarea instrumentelor pentru membrii"
                   FontSize="14"/>
            <Label Text="✔ Trimiterea de notificări pentru programări"
                   FontSize="14"/>
            <Label Text="✔ Feedback pentru lecțiile susținute"
                   FontSize="14"/>

            <Label Text="🔹 Dezvoltator: Sara Marta Vrencean"
                   FontSize="16"
                   FontAttributes="Bold"
                   Margin="0,20,0,0"/>
            <Label Text="© 2024 Music School. Toate drepturile rezervate."
                   FontSize="14"
                   HorizontalOptions="Center"/>

            <Button Text="Logout" BackgroundColor="Red" TextColor="White" Clicked="OnLogoutClicked"/>
        </StackLayout>
    </ScrollView>
</ContentPage>